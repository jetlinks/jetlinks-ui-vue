interface:
  display_name: "JetLinks Delivery"
  short_description: "JetLinks 提交信息、测试、公共契约与PR交付规范"
  default_prompt: >-
    使用 $jetlinks-delivery 起草符合规范的中文 commit、shell 安全的提交命令、测试证据与 PR 描述。1)必读：references/commit-message-zh.md（中文 commit 体例）、references/shell-commit-examples.md（按 bash/zsh、PowerShell、cmd 选择安全提交命令）、references/git-and-pr-rules.md（分支策略 + PR 必备字段 + 后端设计与测试驱动门禁 + 测试证据 + 文档同步 + PR 模板）。2)提交标题规范：默认 `type(scope): summary`；type 跟仓库历史（优先 feat / fix / refactor / docs / test）；scope 选真实模块 / 业务域 / 规则域，不要 update / fix bug / misc / test 这种空泛词；summary 中文动宾短语直接说结果，不写"修改了一些代码"。3)分支策略：禁止直接 push 到 master / main / 集成版本分支；新分支命名跟仓库已有风格；必须走 PR 合并；任何受保护分支只能 fast-forward 或经 PR 合并，不能强推。4)shell 提交命令按用户 shell 选择 references/shell-commit-examples.md 中的 here-doc、PowerShell here-string、临时文件或 `git commit -F -` 等形式，避免 `"`、`$`、反引号和字面 `\n` 破坏消息；commit 后 `git status` 验证。5)较大后端改动或新功能必须有归属文档中的设计稿、用户明确确认、任务拆分和测试目标；README 只放长期总览，不放测试报告、任务流水或 PR 证据；涉及 CRUD 时必须按 $jetlinks-assets-permission 给出 AssetsHolder 数据权限分析结论，拿不准处应询问用户并在 PR 中说明；涉及复杂业务、公共类、SPI / 扩展点、Provider、Codec、Listener 或 CommandHandler 时，必须说明注释 / 公共契约结论，类注释和 SPI 方法注释完整，SPI 必要 @since / @see 指向真实版本、订阅相关类型或参考实现；必须检查 touched backend files，复杂业务、权限、兼容、生命周期、TraceHolder / MBean、协议 wire、事件订阅、跨边界契约等需要注释时，注释必须落到代码旁边，不能只写在 PR 描述；涉及关键后端业务链路、状态流转、命令执行、事件 / 订阅、协议链路、批处理或外部 I/O 时，必须说明 TraceHolder / MonoTracer / FluxTracer 埋点目标、关键属性、上下文传播和敏感信息排除，不新增埋点时说明已有平台自动追踪或不适用依据；涉及常驻内存任务、缓存、队列、buffer、重试池、会话 / 连接 / 订阅管理器或后台执行器时，必须说明 MBean 决策、统计指标、监控字段、安全内部操作、生命周期和敏感信息排除，不新增 MBean 时说明已有覆盖或不适用依据；涉及复杂 SQL / 原生 SQL / 聚合 / 多表 / 深分页 / 批量写入时，必须说明标准 SQL 兼容性、特定数据库方言风险、真实数据规模和压测目标。6)后端新增功能或既有功能变动必须补齐对应单元测试和单元测试覆盖率；涉及数据库、消息、事件、协议、跨模块边界、外部依赖或启动装配时补集成测试或给出结果，未触发时写明不适用原因；涉及复杂 SQL 或批量数据库写入时补压力测试或等价性能验证，无法执行时写明阻塞和剩余风险；测试目标必须对应真实使用场景和真实数据，不允许为了过测删除测试、弱化断言、只 mock 核心逻辑或只跑无关模块。7)交付前自检：单元 / 集成 / 压力测试是否跑过、覆盖率是否有数字、注释 / 公共契约结论是否已说明且必要注释已落到代码中、SPI 必要 @since / @see 是否真实且有助于定位相关类型、TraceHolder 链路追踪决策是否已说明、MBean 运维可观测性决策是否已说明、设计稿 / 相关文档（SKILL.md / references / openai.yaml）是否与实现一致、测试证据是否在 PR / CI 而非 README 中、API / 事件 / 协议 / 配置等 PR 内未发布中间形态是否已收敛到最佳实践、无已发布 / 持久化 / 外部依赖依据的兼容分支 / 旧别名 / 双解析 / 迁移代码 / 旧行为测试是否已删除、是否存在新增不稳定依赖、是否有 SQL 方言 / 性能风险或 root cause / no-hack 红线需要在交付说明中留痕。8)PR 描述默认按本文件中的推荐模板输出，不自创章节，不合并章节；模板内容至少包含：变更摘要（为什么、不是做了什么）、设计与测试目标（设计稿路径 + 用户确认状态 + 测试目标达成情况 + CRUD / AssetsHolder 数据权限结论 + 数据库兼容与 SQL 性能结论 + 注释 / 公共契约结论 + 链路追踪 / 可观测性结论 + MBean / 运维可观测性结论）、测试证据（命令 + 新增/更新测试类 + 通过结果 + 覆盖率数字 + 集成 / 压力测试结果或不适用原因，没法跑明确说明原因）、文档同步情况、风险与回滚、关联 issue / 文档；如属 PR 内未发布逻辑收敛，说明“不涉及已发布兼容性”，如需兼容则写清对象、退出条件和迁移方式；如使用特定数据库方言，写明用户要求、数据库范围、不可移植风险和替代方案。9)高风险动作（反射 / Unsafe / 改可见性 / 复制源码 / monkey patch / 响应式模块阻塞调用 / 新增不稳定依赖 / 特定数据库方言 SQL）必须在 PR 描述显式列出风险点与替代方案讨论结果。10)协同：根因 / no-hack 留痕规则、代码注释 / 公共契约、TraceHolder 埋点边界和 MBean 运维可观测性来自 $jetlinks-conventions；数据权限结论来自 $jetlinks-assets-permission；交付完成后判断是否值得沉淀走 $jetlinks-capture；测试证据缺失时回到对应实现技能补测，缺门禁的后端 PR 不进入 ready for review。
